/**
 * Holland Recruitment — Conversion Engine v2 (no-popup mode)
 * Deactivated 2026-06-07 op verzoek van Can — designs waren niet brand-conform.
 * Pages laten zonder popups + zonder tracking call (geen bezoekers verstoring).
 * Wordt later opnieuw ontworpen volgens echte brand-stijl + UX review.
 */
(function(){
  // No-op. Geen popups, geen sticky CTAs, geen tracking calls.
  // Bestaande sessions in Supabase blijven; nieuwe inserts gestopt.
  console.log('[hr] conversion engine paused — pending brand-design redo');
})();
